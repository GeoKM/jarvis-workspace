# 2026-04-09 — Daily Memory Log

## Retro Computing

- Resumed reconnaissance of the MP/M II system on `dockyards.can.barnabasmusic.com:3002`.
- Confirmed MP/M II uses manual drive + user area exploration rather than named directories.
- Initial read-only survey results so far:
  - `A0` shows no ordinary files but reports `System Files Exist`
  - `A1`, `A2`, `B0`, `B1`, `B2` show `File not found`
  - `C:` and `D:` trigger BDOS bad-sector errors when transient commands like `USER`, `DIR`, or `STAT` are invoked
- Keith advised that `SDIR` can be used on MP/M to list system files, so `SDIR` on `0A:` should reveal the system files on drive A.
