# 2026-04-07 — Daily Memory Log

## linux-system-monitor — major rework

- Replaced `collect-metrics.sh` with `collect-metrics.py` (pure Python, avoids all bash/heredoc quoting issues)
- Remote execution: pipes `collect-metrics.py` over SSH instead of bash heredoc
- Docker monitoring added: running/total, unhealthy, restarting, per-container name+status+CPU+MEM, top-3 lists
- `int_or_0()` now handles scientific notation (`2.14748e+09`) via `float()` first
- `MemAvailable` capped at `MemTotal` (fixes Proxmox virtual address space over-reporting)
- `parse-report.py`: added missing `check_security()`, fixed `swap_used` KeyError, fixed Docker bool/string comparison
- 8 helper encoding scripts: disks, net, tcp, temp, services, logins, ntp, containers

## lab-docs GitHub repo created

- Initialised `~/lab-docs/` as its own private GitHub repo: `https://github.com/GeoKM/lab-docs`
- Visibility: PRIVATE
- Committed to `master`
- Contains: monitoring hosts inventory and per-host override YAMLs
- `.gitignore` excludes: snapshots/, alerts/, cache, editor noise, .env/secret files
- README.md created

## GitHub auth — root cause found and fixed

- Previously broken helper path: `/home/keith/.linuxbrew/bin/gh` (keith vs linuxbrew)
- Fixed: `~/.gitconfig` credential.helper → `!/home/linuxbrew/.linuxbrew/bin/gh auth git-credential`
- Token stored in desktop keyring (GNOME keyring via `gh auth login`)
- PAT expired; `gh auth refresh` renewed it — but PATs don't actually refresh, a new token is needed
- Correct auth helper confirmed working: `printf 'protocol=https\nhost=github.com\n' | git credential fill` returns correct creds

## linux-system-monitor — hosts added today

New hosts added (all ssh: true, user: keith, identity: ~/.ssh/id_jt_ed25519):
- `cbm.can.barnabasmusic.com` — 2 cores, 3.3 GiB RAM, Debian, no Docker
- `kasm.can.barnabasmusic.com` — 4 cores, 4 GiB RAM, Proxmox 6.17.13-2-pve, 11 Docker containers, zombie process noted
- `tg-b.can.barnabasmusic.com` — 1 core, Twingate connector, Proxmox 6.17.13-2-pve, 237h uptime
- `arya.can.barnabasmusic.com` — 2 cores, fresh (0.2h uptime), ZFS `_export_LibraryPool_Archive` 89.1% used WARNING
- `proxima.can.barnabasmusic.com` — 8 cores, 7.7 GiB RAM, PVE 6.17.13-2-pve, MemAvail 25.6% (swap heavy), ceph-osd top MEM
- `proxima2.can.barnabasmusic.com` — 8 cores, 7.7 GiB RAM, PVE 6.17.13-2-pve, MemAvail 34.5%, ceph-osd top MEM
- `proxima3.can.barnabasmusic.com` — 2 cores, 7.6 GiB RAM, PVE 6.17.13-2-pve, `_PX3_Data0_subvol-101-disk-0` 89.1% used WARNING
- `proxima4.can.barnabasmusic.com` — 6 cores, 15.5 GiB RAM, PVE 6.17.13-2-pve, MemAvail 10.2%, kvm 57% top MEM (this is Hebei VM)
- `proxima5.can.barnabasmusic.com` — 4 cores, 15.5 GiB RAM, PVE 6.17.13-2-pve, MemAvail 65.3%, ceph-osd top MEM

Per-host override YAMLs created for all 5 proxima nodes (network.drops_warn=2M, errs_warn=100).
All 14 hosts now in monitoring.

## Proxmox cluster topology (from Keith)

- proxima/proxima2: RAM-constrained cluster nodes; Keith is looking for more memory
- proxima3: runs Arya (Archive Server VM) — the 89.1% ZFS dataset is expected/normal for this role
- proxima4: runs Hebei (Keith's workstation) as KVM VM — 57% RAM consumption is expected
- proxima5: cluster node

## Telegram group — Retro Computing

- Group created and bot added as admin
- Bot can read group messages (verified via `getChat` + `getChatMember` API)
- `requireMention: true` was blocking responses because Telegram `@mention` entity must be inserted via autocomplete, not just text matching
- Fix applied: `groups."*".requireMention: false` in openclaw.json (changes section)
- Gateway reloaded with new config

## Commit log (workspace)

- `93ba78f` — linux-system-monitor initial Docker support
- `28e99f7` — MEMORY.md and skill docs (all 5 new hosts)
- `9c03416` — MEMORY.md gh auth diagnostic notes
- `b54b0e3` — lab-docs repo creation
