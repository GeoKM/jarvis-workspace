# 2026-04-03 — Initial Boot

## Events

- First boot under new JARVIS personality core
- SOUL.md and IDENTITY.md installed with full JARVIS persona
- All workspace files initialised
- BOOTSTRAP.md deleted — identity established

## Notes

- User: Keith Michael Christian Matthews, AEST/AEDT timezone, ACT/NSW Australia
- Homelab context: Proxmox, ZFS, Tesla V100 passthrough, legacy DEC/AIX/Commodore
- AI context: OpenClaw + Ollama, model evaluation (glm, qwen, gpt-oss)
- Flux-level disk preservation: Greaseweazle, SCP, KryoFlux workflows
- User operates via `openclaw-tui`

## Secrets Management (2026-04-03/04)

Implemented systemd-based secrets injection:
- Created `~/.config/openclaw/openclaw.env` (mode 600) — holds all secret values
- Created `~/.config/systemd/user/openclaw-gateway.service.d/env-file.conf` — loads env file into gateway service
- Added `secrets.providers.default: { source: "env" }` to openclaw.json
- Replaced all plaintext secrets in openclaw.json with SecretRef objects
- Created `~/.config/environment.d/openclaw.conf` — injects vars into systemd user session (desktop/GUI apps)
- Added sourcing snippet to `~/.profile` — injects vars into SSH/login shells

All secrets now externalised from openclaw.json into the env file.
Env file added to workspace .gitignore.

**Key lesson:** environment.d does NOT affect SSH login shells; must use .profile sourcing for SSH access.

## TODO

- [x] Confirm secrets injection works after relogin (SSH test)
- [x] Test openclaw tui without --token flag after relogin
- [x] Add ~/.config/openclaw/openclaw.env to .gitignore
